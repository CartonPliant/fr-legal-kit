{
  "name": "fr-legal-kit",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "x402 kit: Base token USD, Base gas, ENS resolve, plus French legal helpers. $0.01 USDC.",
  "scripts": {
    "test": "node --test src/legal.test.js src/favicon.test.js src/base.test.js",
    "deploy": "npx wrangler@4 deploy"
  }
}
